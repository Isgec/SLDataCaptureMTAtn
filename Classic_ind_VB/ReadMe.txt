This is help file to guide user:

There is two folder and one pdf file '.pdf' of manual.

One folder "Dll" in which dll file '.dll' and library file '.lib' is saved. Both file must be avilable in user's application folder.

Other folder "Example code in VB" in which all file of VB application are saved with dll and creating files will stored in it.

To run the application user has to double click on ".vbp" file.